import { Manifest } from "@zachary_masson/modular-core";

/*          Imports Mapping         */

// Commands

// Events

// ContextMenus

// Buttons

// Entities


const manifest: Manifest = new Manifest({
  name: "error_manager",
  description: "Module for manage all error",
  author: "Zachary Masson <contact@zacharymasson.com>",
  mappings: {
    commands: [],
    events: [],
    context_menus: [],
    buttons: [],
    entities: [],
  }
});

export = manifest;
